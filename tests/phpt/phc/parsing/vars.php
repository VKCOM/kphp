@unsupported
<?php
	$x;
	$$x;
	${$x};
	$$$x;
	${$x . $y};
	${f()};
	
	$x->y;
	$$x->y;
	${$x}->y;
	$$$x->y;
	${$x . $y}->y;
	${f()}->y;
	
	$x->$x;
	$$x->$$x;
	${$x}->${$x};
	$$$x->$$$x;
	${$x . $y}->${$x . $y};
	${f()}->${f()};
	
	$x->$x->y;
	$$x->$$x->y;
	${$x}->${$x}->y;
	$$$x->$$$x->y;
	${$x . $y}->${$x . $y}->y;
	${f()}->${f()}->y;
	
	C::$x;
	C::$$x;
	C::${$x};
	C::$$$x;
	C::${$x . $y};
	C::${f()};

	${$x[5]};
	$$x[5];
?>
