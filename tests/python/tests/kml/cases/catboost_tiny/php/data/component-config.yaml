entry: script
components:
  script:
    image: KPHP
    scope: Request
    args:
      ini kml_dir: ${KPHP_KML_DIR}
    links:
      instance_cache: instance_cache
  
  instance_cache:
    image: internal/cache
    scope: Global
    args: {}
    links: {}
