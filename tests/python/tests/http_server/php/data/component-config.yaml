entry: script
components:
  script:
    image: KPHP
    scope: Request
    args:
      ini hello: "world"
      runtime-config: ${RUNTIME_CONFIG_PATH}
      cluster-name: "custom_cluster_name"
    links: {}
