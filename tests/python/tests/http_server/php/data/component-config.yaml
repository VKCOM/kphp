entry: script
components:
  script:
    image: KPHP
    scope: Request
    args:
      ini hello: "world"
      runtime-config: "{\"name\": \"simple configuration\", \"version\": \"1.0.0\"}"
    links: {}
