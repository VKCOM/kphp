cluster A {
  crypto;
  server 127.0.0.1:${PORT}?0.1;
  schema any;
  proto tcp;
  extension round_robin_balance {
    lease_time 60;
  }
}
cluster A {
  id 1;
  crypto;
  server 127.0.0.1:${PORT}?0.1;
  schema any;
  proto tcp;
  extension round_robin_balance {
    lease_time 60;
    local;
  }
}
