#pragma once

#include <string>
#include <vector>

size_t total_mem_used __attribute__ ((weak));

using std::vector;
using std::string;

bool use_safe_integer_arithmetic __attribute__ ((weak)) = false;

